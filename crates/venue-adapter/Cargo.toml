  [package]
  name = "venue-adapter"
  version = "0.1.0"
  edition = "2021"

  [dependencies]
  venue-core = { path = "../venue-core" }
  async-trait = "0.1"
  tokio = { version = "1", features = ["sync"] }